name: Build Android App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      # Java 21 (Compatível com sua Keystore)
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Sync Capacitor
        run: npx cap sync android

      # 1. Decodifica a Keystore dos Secrets para um arquivo real
      - name: Decode Keystore
        env:
          ENCODED_STRING: ${{ secrets.ANDROID_KEYSTORE_FILE }}
        run: |
          echo $ENCODED_STRING > keystore-b64.txt
          base64 -d keystore-b64.txt > release.jks

      # 2. Constrói o APK (Release)
      - name: Build Android Release
        run: cd android && ./gradlew assembleRelease

      # 3. Assina Manualmente (Usando ferramentas novas 34.0.0)
      - name: Sign APK Manually
        run: |
          # Define caminhos das ferramentas modernas
          BUILD_TOOLS_VERSION="34.0.0"
          ANDROID_SDK_ROOT=${ANDROID_SDK_ROOT:-/usr/local/lib/android/sdk}
          ZIPALIGN="$ANDROID_SDK_ROOT/build-tools/$BUILD_TOOLS_VERSION/zipalign"
          APKSIGNER="$ANDROID_SDK_ROOT/build-tools/$BUILD_TOOLS_VERSION/apksigner"
          
          # Caminhos dos arquivos
          APK_PATH="android/app/build/outputs/apk/release/app-release-unsigned.apk"
          ALIGNED_APK="app-release-aligned.apk"
          SIGNED_APK="app-release-signed.apk"

          echo "🔍 Alinhando APK..."
          $ZIPALIGN -v -p 4 $APK_PATH $ALIGNED_APK

          echo "✍️ Assinando APK..."
          $APKSIGNER sign --ks release.jks \
            --ks-key-alias "${{ secrets.ANDROID_KEY_ALIAS }}" \
            --ks-pass "pass:${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" \
            --key-pass "pass:${{ secrets.ANDROID_KEY_PASSWORD }}" \
            --out $SIGNED_APK \
            $ALIGNED_APK

          echo "✅ APK Assinado com Sucesso!"

      # 4. Upload do arquivo final
      - name: Upload Signed APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release-signed
          path: app-release-signed.apk
